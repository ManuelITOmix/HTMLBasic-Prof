(() => {
/*console.log('suma',utils.sumar(3,5));
console.log('resta',utils.restar(6,3));
console.log('factorial',utils.factorial(5));

console.log('dividir',utils.dividir([5,2]));

console.log('dividir',utils.dividir([5,2]));

const fnCallback = (a,b) => {
    const sumaCuadrados = Math.pow(a,2) + Math.pow(b,2);
    return Math.sqrt(sumaCuadrados);
};

console.log('operacion fn -> ',utils.operar(3,5,utils.sumar));
console.log('operacion fn -> ',utils.operar(3,5,fnCallback));
*/
})();
